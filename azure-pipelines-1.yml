# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  name: 'Atwepool'

steps:
- script: echo "Running a job on my custom agent"
  displayName: "My Custom Job"


